NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api/v1

# Only needed for real Meta OAuth (see backend/.env.example for the matching
# META_APP_ID/META_LOGIN_CONFIG_ID). Leave blank to keep account "connection"
# in sandbox/demo mode.
NEXT_PUBLIC_META_APP_ID=
NEXT_PUBLIC_META_LOGIN_CONFIG_ID=
